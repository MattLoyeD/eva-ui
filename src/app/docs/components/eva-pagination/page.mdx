import { EvaPagination } from "@/components";
import { ComponentPreview, PropsTable, Callout } from "@/components/docs";
import { PaginationBasicDemo, PaginationGreenDemo, PaginationCyanDemo } from "@/components/docs/EvaPaginationDemo";

# Eva Pagination

LCD-style page navigation with `<<` / `>>` arrow buttons and zero-padded page counter. Buttons disable automatically at boundaries.

```tsx
import { EvaPagination } from "@mattloyed/eva-ui";
```

## Basic Usage

<Callout type="info" title="INTERACTIVE">
  Click the arrow buttons to navigate between pages.
</Callout>

<ComponentPreview
  title="DEFAULT"
  code={`import { useState } from "react";
import { EvaPagination } from "@mattloyed/eva-ui";

const [page, setPage] = useState(1);

<EvaPagination
  total={120}
  pageSize={10}
  currentPage={page}
  onPageChange={setPage}
/>`}
>
  <PaginationBasicDemo />
</ComponentPreview>

## Color Variants

<ComponentPreview
  title="GREEN"
  code={`import { EvaPagination } from "@mattloyed/eva-ui";

<EvaPagination
  total={80}
  pageSize={10}
  currentPage={page}
  onPageChange={setPage}
  color="green"
/>`}
>
  <PaginationGreenDemo />
</ComponentPreview>

<ComponentPreview
  title="CYAN"
  code={`import { EvaPagination } from "@mattloyed/eva-ui";

<EvaPagination
  total={50}
  pageSize={10}
  currentPage={page}
  onPageChange={setPage}
  color="cyan"
/>`}
>
  <PaginationCyanDemo />
</ComponentPreview>

## Props

<PropsTable
  props={[
    { name: "total", type: "number", required: true, description: "Total number of items" },
    { name: "pageSize", type: "number", required: true, description: "Items per page" },
    { name: "currentPage", type: "number", required: true, description: "Current active page (1-indexed)" },
    { name: "onPageChange", type: "(page: number) => void", required: true, description: "Called when page changes" },
    { name: "color", type: '"orange" | "green" | "cyan"', default: '"orange"', description: "Color theme" },
    { name: "className", type: "string", default: '""', description: "Additional CSS classes" },
  ]}
/>
